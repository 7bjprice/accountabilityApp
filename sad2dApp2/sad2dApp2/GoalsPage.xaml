<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="sad2dApp2.GoalsPage"
             Title="Goals">

    <Grid RowDefinitions="*,Auto">

        <!-- Scrollable main content -->
        <ScrollView Grid.Row="0">
            <VerticalStackLayout Padding="20" Spacing="20">

                <!-- Header -->
                <Border Stroke="LightGray"
                StrokeThickness="1"
                Background="LightGreen"
                Padding="20"
                Margin="10"
                StrokeShape="RoundRectangle 12">
                <Border.Shadow>
                <Shadow Brush="Black"
                        Offset="5,5"
                        Radius="10"
                        Opacity="0.3" />
                </Border.Shadow>
                <VerticalStackLayout>
                <Label Text="Inspirational Quote"
                    FontSize="18"
                    TextColor="White"
                    FontAttributes="Bold"
                    Margin="0,0,0,10" />
                <Label x:Name="QuoteLabel"
                    Text="Loading..."
                    FontSize="16"
                    FontAttributes="Italic"
                    TextColor="White"
                    HorizontalOptions="Center"
                    HorizontalTextAlignment="Center"
                    Margin="10,0" />
                <Label x:Name="TotalGoalsLabel"
                    Text="Set Your Why"
                    FontSize="28"
                    FontAttributes="Bold"
                    TextColor="White"
                    HorizontalOptions="Center"
                    Margin="0,10,0,0"/>
                    </VerticalStackLayout>
                </Border>

                <!-- Set Overall Goals Button -->
                <Button x:Name="SetGoalsBtn"
                        Text="Set Your Reason Why"
                        BackgroundColor="#4CAF50"
                        TextColor="White"
                        CornerRadius="10"
                        HeightRequest="40"
                        Clicked="OnSetGoalsClicked"
                        Margin="0,0,0,10"/>

                <!-- Goals Categories -->
                <CollectionView x:Name="GoalsList">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border Stroke="#DDDDDD"
                                    StrokeThickness="1"
                                    Background="#F5F5F5"
                                    Padding="15"
                                    Margin="5"
                                    StrokeShape="RoundRectangle 10">
                                <Border.Shadow>
                                    <Shadow Brush="Black"
                                            Offset="2,2"
                                            Radius="6"
                                            Opacity="0.2" />
                                </Border.Shadow>

                                <!-- Use Grid for layout <Label Text="{Binding Category}" {Binding Amount, StringFormat='${0:F2}'}  -->
                                <Grid ColumnDefinitions="*,Auto">
                                    <Label Text="{Binding Category}"
                                           FontSize="18"
                                           VerticalOptions="Center"
                                           Grid.Column="0" 
                                           TextColor="Black"/>

                                    <Label Text="{Binding Amount, StringFormat='${0:F2}'}"
                                           FontSize="18"
                                           FontAttributes="Bold"
                                           VerticalOptions="Center"
                                           HorizontalTextAlignment="End"
                                           Grid.Column="1" 
                                           TextColor="Black"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <!-- Add Goals Item Button -->
                <Button Text="Add New Goals"
                        BackgroundColor="#2196F3"
                        TextColor="White"
                        CornerRadius="10"
                        HeightRequest="50"
                        Clicked="OnAddGoalsItemClicked" />

            </VerticalStackLayout>
        </ScrollView>

        <!-- Bottom nav bar -->
        <Grid Grid.Row="1" ColumnDefinitions="*,*,*">

            <Button Text="Budgets"
                    Grid.Column="0"
                    HeightRequest="80"
                    FontSize="Header"
                    Clicked="OnBudgetClicked" />

            <Button Text="Home"
                    Grid.Column="1"
                    HeightRequest="80"
                    FontSize="Header"
                    Clicked="OnMainClicked"/>

            <Button Text="Goals"
                    Grid.Column="2"
                    HeightRequest="80"
                    FontSize="Header"
                    Clicked="OnGoalsClicked" />
        </Grid>

    </Grid>
</ContentPage>
